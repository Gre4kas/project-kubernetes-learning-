apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-kuber.fullname" . }}
  labels:
    {{- include "test-kuber.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-kuber.selectorLabels" . | nindent 4 }}
